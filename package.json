{
  "name": "discord-sheets-logger",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix UI install && npm --prefix UI run build",
    "start": "node index.js"
  },
  "dependencies": {
    "discord.js": "^14.19.3",
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0"
  }
}
